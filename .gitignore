__pycache__/
*.pyc
*.bak-*
*.pre-sqlite-backup
bot.pid
bot.out.log
dashboard.pid
dashboard.out.log
wss_listener.pid
wss_listener.out.log
watchdog.out.log
data/watchdog_paused
*.pre-cutover-*
.claude/settings.local.json

# Node / TS monorepo
node_modules/
.pnpm-store/
apps/dashboard/.next/
apps/dashboard/out/
*.tsbuildinfo
packages/*/dist/

# Shared SQLite DB (local dev data, not source)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal

# Secrets / credentials — never belong in git (2026-07-29: polymarket-key.pem
# and AWSCLIV2.pkg were accidentally committed and pushed to a public GitHub
# remote; history was purged and the EC2 key pair rotated, see
# docs/copy-trading/SAFETY.md)
*.pem
*.ppk
AWSCLIV2.pkg
*.pkg

# env
.env

# Orchestrator runtime state (machine-local cadence bookkeeping, not source)
packages/weather/data/
.env.local
