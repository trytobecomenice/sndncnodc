# Tomorrow TDL — 2026-08-06

Read `CLAUDE_HANDOFF.md` first. The 2026-08-05 quant-control code is committed/tested locally but
must not be treated as production-active until the deployment checks below are complete.

AWS `data/autodeploy.lock` remains intentionally in place. A narrow TTP parser hotfix is live as
commit `328a22d`, but the larger quant-control code on `origin/main` is still not production-active.
The current paper bot should remain on `328a22d` overnight. Confirm that state first; remove the
lock only during the controlled deployment below.

As of 2026-08-06 01:30 HKT, GitHub `main` also includes passive shadow/panic implementation commit
`d5ae7a4`; it is covered by 727 local tests but is likewise **not production-active**.

Deployment update, 2026-08-06 19:01 HKT: the AWS checkout is now `0f97c4e`, but the existing paper
bot PID `83881` was deliberately not restarted and therefore still runs its pre-deployment loaded
code/config. `data/autodeploy.lock` remains present. Only the isolated, read-only
`phase0-soak-recorder.service` was activated. Do not describe the larger main-bot changes as live.

## P0 incident override — 2026-08-07 paper-ledger integrity

This section supersedes feature work below until complete. An external quant review found strong
evidence that replayed/post-event Paper entries created near-instant `resolved` profits and then
polluted wallet EV, caps, mute/promotion decisions, and equity HWM. Codex independently reproduced
the central finding read-only on AWS on 2026-08-07; the reviewer's exact local decomposition was
not the authoritative AWS result. Use
`docs/research/PAPER_LEDGER_INTEGRITY_INCIDENT_2026-08-07.md` as the canonical evidence and
acceptance record.

1. Freeze claims based on historical reported PnL; remain Paper-only and do not expand roster,
   strategy scope, bankroll, or order size.
2. **Completed read-only and isolated:** AWS raw 641 / `+$620.40`; causal v1 candidates 455 /
   `+$669.00`; factual resolution audit then found another 31 / `+$29.90` among the v1 remainder.
   Fact-clean row ledger is 155 / `-$78.50` / `-9.07%`; zero resolution verdicts were unknown.
   Laptop and AWS remain separate.
3. **Completed locally:** versioned causal classifier with row IDs, reasons, SHA-256 manifest,
   dry-run default, and mark-only apply. It does not use `<600s` as a blanket rule and never
   deletes/rebases history.
4. **Read-only reconciliation completed; durable accounting now P0:** 14,614/14,614 realized
   events allocated to exactly one closed/open tax lot; unmatched=0, ambiguous=0. Clean cumulative
   realized is `-$20.369734` (`-$20.364972` closed plus `-$0.004762` open-lot partials), versus
   phantom event PnL `+$717.385620`. Add a persisted event-to-tax-lot allocation/cumulative-realized
   ledger, backfill it under a manifest, and make runtime snapshots subtract event allocations—not
   final-row PnL—before treating snapshot/HWM realized inputs as correct.
5. **Completed/deployed:** confirmed rows are excluded from wallet EV, replacement ranking,
   rolling mute evidence, dashboards, open-state loading, and clean readers. v1 marked 455 rows;
   factual v2 marked 31 more under manifest `a2b4c7…dd820`. Do not change wallet statuses
   automatically; current eligible evidence has effective wallet N=6 and clustered CI crosses zero.
6. **Completed urgent operations fix:** watchdog PID-file race created duplicate Paper processes
   at 18:00 and 18:24 UTC. Duplicates were terminated; commit `164c7bd` adds process discovery,
   atomic PID repair, start serialization, and duplicate fail-closed alerting. AWS live simulation
   removed `bot.pid`; the code recovered PID `105229` without starting another process.
7. Audit TTP pricing with success/failure denominators, fix any batch-aborting type path, add
   deduplicated SLO alerts, and quarantine persistently unresolvable markets.
8. Enforce Shadow Rehab exclusion inside risk-manager boundaries and audit its lifecycle closure,
   age, pricing, and cost units.
9. Add conservative-equity and unpriceable-cost evidence for drawdown decisions.
10. Promote bounded early-rejection/foregone-opportunity capture to P0 research because frequent
   capital saturation can reduce allocation to arrival order.
11. Only after clean evidence exists: run prospective exit-policy research and observe corrected
    bankroll-based Kelly mapping without raising the `$3–$10` bounds.

Controlled AWS application gate for items 3–5: backup `data/app.db`; preserve the autodeploy lock;
apply Drizzle migration `0023`; run the classifier dry-run and compare manifest/aggregates; only
then mark rows, clear the stale HWM ratchet for a clean reseed, restart exactly one Paper process,
and verify two TTP cycles. The isolated Phase-0 recorder must not be interrupted.

**Deployment update — 2026-08-07 01:44 HKT:** gate completed through the first TTP/equity cycle.
Verified 4.9 GB SQLite backup made with Python `sqlite3.Connection.backup()`, exact schema
hash/count match, AWS targeted P0 regressions 11/11,
manifest match `75755957…`, 455 marked closed rows, zero marked open rows, clean HWM reseed
`$1,112.60`, 52/52 fresh open marks, zero post-restart errors, Paper PID `105229`, recorder active,
watchdog restored, and autodeploy lock retained. `pnl_snapshot` now has 22 rows, but its event-log
realized total does not reconcile to final rows and the loop is I/O-heavy. Drizzle migration 0023
was applied directly and journaled as the new baseline;
destructive Weather-removal migration 0022 was intentionally skipped and all nine legacy Weather
tables were preserved.

## P0 — production preflight and controlled deployment

1. Reconcile the two intentional EC2-only `config.py` overrides into a deployment-safe config
   mechanism; do not overwrite `TRACKED_TRADERS_SOURCE="db"` or `ENABLE_OMS_SHADOW_MIRROR=True`.
2. Back up `data/app.db`, pause the watchdog, confirm the existing autodeploy lock, fast-forward
   the reviewed commit, run the tracked Python suite, and restart exactly one paper-mode `bot.py`
   process. Remove the autodeploy lock only after post-restart verification passes.
3. Verify startup circuit-breaker re-evaluation. Expected from the 2026-08-05 read-only audit:
   `strict-5` must mute for ten identical -100% returns; independently inspect whether
   `yield-farmer-1` also mutes from its saved t-stat around -3.18.
4. Verify `bot_risk_state.clean_evaluation_epoch` is created once and survives a second restart.
   Do not delete/rebase historical PnL.
5. Wait for two TTP cycles and confirm `pnl_snapshot` receives both `portfolio` and `clean_epoch`
   rows about five minutes apart, with fresh marks and no new errors.
6. Confirm the 75% drawdown warning transition, Telegram alert, and BUY-only soft pause using
   controlled test state if production equity does not naturally cross it. SELL/closeout paths
   must remain available; recovery below 60% must clear the pause once.
7. Reconfirm the `last_trade_price=""` hotfix remains clean after the main deployment: two TTP
   sweeps, all normal open positions freshly marked, and zero new `could not convert string to
   float: ''` errors.

## P1 — challenger pipeline activation

1. Run the scorer jobs and `python3 manage_challengers.py` once manually; inspect every proposed
   `watch -> challenger` transition before adding it to cron.
2. Confirm challengers are polled within five minutes without restart, create only
   `strategy='shadow_challenger'` rows, and never create `bot_filtered` exposure.
3. Add `python3 manage_challengers.py` between the daily scoring jobs and
   `send_wallet_approvals.py` in the EC2 cron only after the manual dry run is clean.
4. Do not approve a real promotion until one challenger has at least 7 days and 20 non-dust
   closes with a positive one-sided lower confidence bound. Then verify approval atomically sets
   the challenger to `track` and the selected muted wallet to `retiring`.
5. Verify retiring wallets reject BUYs, still process existing exits, and move to `watch` only
   after all real open positions are gone.

## P2 — operator view and 24-hour review

1. Add dashboard cards for clean-epoch realized/unrealized PnL, epoch timestamp, drawdown dollars,
   warning state, challenger evidence, and pending one-in-one-out replacement.
2. Review 24 hours of snapshot cadence, event volume, new mute events, challenger rows, and DB
   growth before changing any threshold or enabling live trading.
3. Keep `MIN_TRADE_USD=$3` and `MAX_TRADE_USD=$10`; Joey explicitly declined the temporary $5 cap.

## P3 — lean shadow recorder and replay evidence

Follow `docs/research/SHADOW_REPLAY_ARCHITECTURE_2026-08-06.md`; do not begin with continuous
full-depth capture on the current `t3.small`.

1. **DONE (Phase A foundation):** Define the compact, versioned event envelope plus BBO/top-three and executable VWAP fields for
   `$3`, `$5`, and `$10`. Include source-impact, self-inflicted latency, environment snapshot, and
   data-quality fields from day one.
2. **PARTIAL:** Journal -> virtual-clock -> deterministic hypothetical BUY replay is implemented.
   Add the numeric source-impact/delay/spread/fee attribution decomposition before multi-day
   collection.
3. **POLLING PATH DONE; STANDALONE PUBLIC WS SOAK ACTIVE:** The bounded recorder now consumes real
   polling source signals and reuses the exact direct-REST depth book to record hypothetical
   decisions; both in-process feature flags default off and no order can be submitted by the shadow
   module. A separate public-WS/wallet recorder began its AWS soak at 2026-08-06 19:00 HKT under
   384 MB/512 MB memory and 35% CPU caps. Inspect its first 24 hours after 2026-08-07 19:00 HKT;
   this is evidence collection, not proof of burst safety or alpha.
4. **PARTIAL:** The immediate-trip/hysteretic-recovery controller, sole BUY-gate consumer, queue
   overflow counter, writer-error health, passive signal queue age, and passive book freshness now
   exist. HTTP ingress is stamped before JSON decode, parse duration is explicit, and the safety
   age includes ingress-to-decision rather than only post-parse queue time. Heavy envelope/VWAP/JSON
   construction is deferred out of the synchronous BUY call stack. There is deliberately no
   high-frequency watchdog polling loop. Add recorder CPU/RSS and AWS CPU-credit observability at
   low cadence, and benchmark burst-time scheduling latency.
   The recorder must degrade capture before it can delay trading. Execution-integrity `CRITICAL`
   conditions engage a hysteretic BUY/entry interlock while exits remain available; optional
   recorder backpressure alone does not trigger the persistent capital hard kill.
5. **DONE LOCALLY; NOT DEPLOYED:** Malformed local portfolio/risk invariants trigger a persistent
   panic hard kill, invalidate delayed entry intents, preserve SELL exits, and alert Telegram.
   Do not replace this with indiscriminate cancel-all: current managed resting orders are exits,
   while live BUY uses FAK/market semantics. Authenticated venue reconciliation remains manual.
6. Collect 3-7 days, then decide from net copy alpha and p95/p99 host latency whether to continue
   high-frequency copy research, pivot to lower-frequency signals, or upgrade AWS.
7. Preserve a language-neutral schema for a future C++ book/recorder hot path, but rewrite only
   after profiling. Fixed-performance/separate compute comes before bare metal or kernel bypass.
8. Benchmark stdlib `json`, `orjson`, and a typed decoder against captured production payloads;
   require semantic-equivalence, malformed-input, p99 latency, and peak-RSS gates before changing
   decoder dependencies.
9. **DECISION CHECKPOINT LIVE PATH WIRED; OTHER CHECKPOINTS PENDING:** Journal `$3/$5/$10` VWAP at source-pre-trade,
   signal-visible, decision-commit, and execution checkpoints, each tied to book sequence/hash and
   timing-uncertainty metadata.
10. **BLOCKING GATE BEFORE PUBLIC WS:** the current bounded writer is a Python thread and still
    shares the GIL. Capture real volatile-market WS frames with an independent process/host, replay
    their original inter-arrival pattern at 1x/5x/10x over a real network path, then move the
    per-message recorder behind a separate process boundary before claiming burst safety.
11. Write protocol v1 plus C++/Python golden vectors before building the sidecar: Unix-domain
    `SOCK_STREAM`, bounded length framing, reconnect epoch/sequence, event-armed coalesced push,
    canonical e6 fixed-point, explicit rounding/overflow, and malformed-frame fail-closed tests.
12. Add an early-rejection capture path. If capital is unavailable or the entry gate is closed,
    journal only the bounded raw signal/BBO/generation/reason marker; compute EV, Kelly, capital
    velocity, and foregone EV offline in replay rather than burning Python hot-path CPU.
13. Extend the future order/reservation state machine with `orphaned` and reconciled terminal
    outcomes. HTTP timeout must freeze capital and trigger idempotent order/trade/open-order/
    balance/position reconciliation; never infer finality from expected holding time.
14. Rename latency fields to match what is actually observed. Python records local monotonic
    userspace spans and RTT; socket-write return is not kernel/NIC handoff, and exchange wall time
    must not be subtracted from local monotonic time. Treat `SO_TIMESTAMPING`/eBPF as an optional
    later measurement layer.

## P4 — strategy evidence and expansion gates

Follow `docs/research/STRATEGY_AND_EXPANSION_DECISIONS_2026-08-06.md`. This is a research sequence,
not authorization to enable live trading or add new bots to this repository.

1. **PARTIAL, RECORDER IMPLEMENTED:** Phase 0 now records observed position action separately
   from unknown economic intent, reported source-to-receive time, wallet/model version, chase,
   actual-size executable VWAP, same-request venue fee metadata, and exact shadow gate trace.
   The standalone recorder also preserves distinct incremental fills, BUY/SELL source reductions,
   conservative shadow tax lots, and visibility-lag proxy. Still required: calibrated signal-age
   bounds, source-impact model, conservative residual-edge lower bound, settlement/capital
   discounts, and coverage of signals rejected before the capture seam.
   Do not infer a scalar wallet target or treat an unknown as zero.
2. **PARTIAL, RECORDER IMPLEMENTED:** actual-size entry fill, projected immediate bid-side
   liquidation, and online T+100/T+500 WS observations now exist. Add calibrated
   persistence/survival and stress scenarios. Treat
   fixed spread, percentage-depth, and daily-volume thresholds as shadow buckets until calibrated;
   never report mid/last-trade Paper PnL as realizable equity in a thin book.
3. Define the sparse logical/factor/scenario exposure contract plus shrinkage-covariance metadata.
   Keep dense research and scenario generation offline; execution consumes versioned cached
   headroom and fails conservatively when stale.
4. Specify the proposed first-live Copy execution policy without activating it: price-bounded
   FOK/FAK entries only, no resting Maker Copy entry, urgency-based exits, final price/book/lease/
   alpha-deadline recheck, and separated cancel/exit rate budget.
5. Keep the current `$3-$10` size and Paper-only status until conservative replay, real-burst host
   safety, and a separately approved size-conditioned micro-live protocol all pass. Paper evidence
   can reject a strategy but cannot authorize Live.
6. Only after the Copy gate passes, build a shadow event/payoff knowledge graph and internal
   logical-relative-value scanner. Batch orders are non-atomic; model orphan-leg worst loss and
   rule mismatch before any execution proposal.
7. Evaluate one allowlisted niche primary-source vertical only when its lower-bound alpha half-life
   exceeds end-to-end p99 latency. NLP/LLMs remain untrusted research/extraction components with no
   key, order, or risk-policy authority.
8. Do not begin LP, cross-platform execution, leveraged external hedging, or a multi-strategy
   allocator until each proposed strategy has isolated ledger/risk/kill/attribution and independent
   evidence. These belong in separate services/repositories, not Copy Bot source scope.
